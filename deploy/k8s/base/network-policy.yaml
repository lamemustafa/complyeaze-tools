apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: complyeaze-tools-deny-egress
  labels:
    app.kubernetes.io/name: complyeaze-tools
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: complyeaze-tools
  policyTypes:
    - Egress
  egress: []
